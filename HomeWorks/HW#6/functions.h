#ifndef FUNCTIONS_H
#define FUNCTIONS_H

#include <string>

void printUsageInfo(char* fileName);
bool isPalindrome(char* checkString, bool caseSensitive, bool space);
bool isPalindromeR(std::string checkString);

#endif